On a Linux machine, the following software is needed:
  inkscape
  gnuplot
  latex
  ruby 1.9
  libxml-parser-perl
  m4

PATH=$PATH\:. ; export PATH
git clone git://github.com/bcrowell/fundamentals-of-calculus.git
cd fundamentals-of-calculus
make preflight && make all_figures && make graphs && make book
